Multivariate GPQR
=================

:math:`y = f(X)` where the dimension of :math:`y` is greater than 1.

Default quantile regression with 1-D :math:`y` is already multi-output, as it models multiple quantiles.
Multivariate quantile regression expands this, modeling multiple quantiles of multiple variables at the same time.
